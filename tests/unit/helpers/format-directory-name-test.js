
import { formatDirectoryName } from 'smartdirect-web/helpers/format-directory-name';
import { module, test } from 'qunit';

module('Unit | Helper | format directory name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatDirectoryName([42]);
  assert.ok(result);
});

